import type { JSX } from "react";

const DescQuiz = (): JSX.Element => {
    return (
        <div className="descquiz">
            <div className="descquiz__container">
                <p>It was a very hot day.  My friend and I wanted to go swimming, but we were too tired to walk to the pool.  We decided to stay home and relax instead. We were both very happy with our decision.</p>
            </div>
        </div>
    );
};

export default DescQuiz;